spring:
  config:
    import: amazonS3.yaml

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 20
        jdbc.batch_size: 20

  datasource:
    url: jdbc:postgresql://localhost:5432/team_challenge
    username: postgres
    password: userPassword